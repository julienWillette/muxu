{% extends 'layout.html.twig' %}

{% block content %}
<h1 class="display-4">Shipping</h1>
<table class="table table-responsive{-sm|-md|-lg|-xl} table-bg">
    <thead>
        <tr>
            <th scope="col">Type</th>
            <th scope="col">Cost</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        {% for shipping in shipping %}
        <tr>
            <td>{{ shipping.type }}</td>
            <td>{{ shipping.cost }}€</td>
            <td>
                <a href="/shipping/show/{{ shipping.id }}" class="btn btn-sm my-2 eye-color"><i
                        class="fas fa-eye logo-eye"></i></a>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}